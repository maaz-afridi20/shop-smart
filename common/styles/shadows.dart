import 'package:shop_smart/utils/contants/exports.dart';

class TShadowsStyle {
  static final verticalProducShadow = BoxShadow(
      color: darkGrey.withOpacity(0.1),
      blurRadius: 50,
      spreadRadius: 7,
      offset: const Offset(0, 2));
  static final horizontalProducShadow = BoxShadow(
      color: darkGrey.withOpacity(0.1),
      blurRadius: 50,
      spreadRadius: 7,
      offset: const Offset(0, 2));
}
